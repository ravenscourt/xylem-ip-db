name: Addition Request
description: Request an IP range to be added to a country's list
labels: ["addition"]
body:
  - type: dropdown
    id: country
    attributes:
      label: Country
      options:
        - "IR - Iran"
        - "RU - Russia"
        - "CN - China"
    validations:
      required: true

  - type: input
    id: ip_range
    attributes:
      label: IP or CIDR Range
      description: "A single IP (e.g. 1.2.3.4) or CIDR range (e.g. 1.2.3.0/24)."
      placeholder: "1.2.3.0/24"
    validations:
      required: true

  - type: textarea
    id: reason
    attributes:
      label: Reason
      description: "Why should this range be added to the list?"
    validations:
      required: true
