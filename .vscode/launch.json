{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Generate Lists",
      "type": "debugpy",
      "request": "launch",
      "program": "${workspaceFolder}/scripts/generate.py",
      "cwd": "${workspaceFolder}",
      "console": "integratedTerminal"
    }
  ]
}
